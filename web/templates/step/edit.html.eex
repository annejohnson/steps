<h1>Edit Step</h1>

<%= render "form.html", changeset: @changeset,
                        action: goal_step_path(@conn, :update, @goal, @step) %>

<%= link "Back", to: goal_path(@conn, :show, @goal) %>
